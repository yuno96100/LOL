/* ============================================================
   [SECTION] 로그인/가입 메뉴 렌더링
   ============================================================ */
function bridge() {
    return {
        /**
         * @param {Object|null} userData - 세션에 저장된 유저 데이터
         * @returns {string} 메뉴 텍스트
         */
        render: function(userData) {
            var title = "🔐 [ 소환사 인증 ]";
            var body = "";

            if (!userData) {
                // 비로그인 상태
                body = "1. 🔓 로그인하기\n2. 📝 회원가입\n\n(번호를 입력하세요)";
            } else {
                // 로그인 상태
                body = "👤 [" + userData.info.name + "]님 접속 중\n\n1. 🚪 로그아웃\n2. 🔙 이전 메뉴";
            }

            return "┏" + "━".repeat(12) + "┓\n" + 
                   "  " + title + "\n" + 
                   "━".repeat(16) + "\n" + 
                   body + "\n" +
                   "┗" + "━".repeat(12) + "┛";
        }
    };
}
