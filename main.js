/**
 * [main.js] v3.2.2
 * 초정밀 생존 확인용 (무조건 응답)
 */

function response(room, msg, sender, isGroupChat, replier, imageDB, packageName) {
    // 어떤 메시지가 와도 무조건 반응합니다.
    // 만약 이 답장조차 안 온다면 파일 저장 경로가 틀린 것입니다.
    replier.reply("📢 [시스템] mai지를 수신했습니다!\n내용: " + msg);
}
